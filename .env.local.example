NEXT_PUBLIC_API_URL=/api/pina
# Opcional: URL del backend para rewrites de Next (dev)
# Cambia el puerto si tu backend no corre en 3000
# BACKEND_URL=http://localhost:4000

# Nota:
# - Usa `yarn dev:4011` para levantar el frontend en el puerto 4011.
# - Con NEXT_PUBLIC_API_URL=/api/pina y BACKEND_URL configurado,
#   Next proxyará las llamadas a `${BACKEND_URL}/api/pina/*` evitando CORS
#   y permitiendo cookies HttpOnly con `credentials: include`.